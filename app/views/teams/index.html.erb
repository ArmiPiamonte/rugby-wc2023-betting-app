<h1>Teams</h1>

<% @teams.each do |team| %>
  <div class="container">
    <div class="d-flex">
      <%= image_tag team.flag %>
      <h3><%= team.country %></h3>
    </div>
    <p>World Ranking: <%= team.ranking %></p>
  </div>
<% end %>
